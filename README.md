Yeticave App (frontend).

I am on the way of rewriting the code from HTML to React, excluding php inserts and realizing all logics with React and php. 
- Login form (component) works. Session opens and closes.
